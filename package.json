{
  "name": "update-subscription-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram bot + HTTP API that swaps blocked Marzban host IPs from a reserve pool when a forward VPS daemon reports a TM block.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "telegraf": "^4.16.3",
    "undici": "^6.21.0"
  },
  "engines": {
    "node": ">=20"
  }
}
